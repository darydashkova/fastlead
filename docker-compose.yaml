version: '3'
services:
    frontend:
        build: 
            context: .
            dockerfile: Dockerfile
            args: 
                - VUE_APP_API_URL=https://example.com
                - VUE_APP_SOCKET_URL=wss://example.com:3000
        network_mode: host
        restart: always